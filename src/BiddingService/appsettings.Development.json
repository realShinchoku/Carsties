{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "IdentityServiceUrl": "${IDENTITY_SERVICE_URL}",
  "RabbitMq": {
    "Host": "${RABBITMQ_HOST}",
    "Username": "${RABBITMQ_USERNAME}",
    "Password": "${RABBITMQ_PASSWORD}"
  },
  "ConnectionStrings": {
    "BidDbConnection": "mongodb://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}"
  },
  "GrpcAuction": "${GRPC_AUCTION_URL}"
}
